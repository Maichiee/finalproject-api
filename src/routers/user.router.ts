// import express from "express"
// import { getProfile, updateProfile, changePassword } from "../controllers/user.controller"
// import { authenticate } from "../middlewares/auth.middleware"
// import { validateProfileUpdate, validatePasswordChange } from "../middlewares/validation.middleware"

// const router = express.Router()

// // All routes require authentication
// router.use(authenticate)

// router.get("/profile", getProfile)
// router.put("/profile", validateProfileUpdate, updateProfile)
// router.post("/change-password", validatePasswordChange, changePassword)

// export default router
